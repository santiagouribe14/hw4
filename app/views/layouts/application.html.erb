<!DOCTYPE html>
<html>
  <head>
    <title>Travel Log</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="/stylesheets/application.css">
  </head>

 <body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3 mb-3">

     <div class="d-flex align-items-center">
        <a class="navbar-brand mb-0">TravelLog |</a>
        <a class="nav-link text-white px-0" href="/">Home</a>
      </div>

      <div>
        <%= flash["notice"] %>
      </div>

      <div class="d-flex ms-auto">
        <% if @current_user == nil %>
          <a class="nav-link text-white px-1" href="/login">Login |</a>
          <a class="nav-link text-white px-1" href="/users/new">Signup</a>
        <% else %>
          <div class="d-flex align-items-center">
            <span class="text-white me-2">
              Logged in as: <%= @current_user["username"] %>
            </span>
            <span class="text-white px-1">|</span>
            <a class="nav-link text-white px-1" href="/logout">Logout</a>
          </div>
        <% end %>
    </div>
  </nav>

  <div class="container">
    <%= yield %>
  </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO" crossorigin="anonymous"></script>
  </body>
</html>
